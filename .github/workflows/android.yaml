name: Android CI/CD & Security Pipeline

on:
  push:
    branches: [ "master", "feat/**" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '0 3 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write
  pages: write
  id-token: write
  security-events: write

jobs:
  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 0: ANÁLISE ESTÁTICA DE SEGURANÇA SAST (CODEQL)
  # ═════════════════════════════════════════════════════════════════════════════

  codeql-analysis:
    name: "🛡️ CodeQL SAST Analysis (Java & Kotlin)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java-kotlin
          queries: security-extended,security-and-quality

      - name: Build Debug Artifacts
        run: ./gradlew assembleDebug

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java-kotlin"

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 1: FUNDAÇÃO
  # ═════════════════════════════════════════════════════════════════════════════

  core-common:
    name: "infra: core-common"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🛡️ Spotless Format & BCV API Check
        run: ./gradlew spotlessCheck apiCheck
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-common:detekt :infra:core-common:lintDebug :infra:core-common:testDebugUnitTest :infra:core-common:assembleRelease :infra:core-common:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-common
          path: |
            infra/core-common/build/reports/tests/
            infra/core-common/build/reports/detekt/
            infra/core-common/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-common
          path: infra/core-common/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-common
          path: infra/core-common/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 2: INFRA BÁSICA (Depende de core-common)
  # ═════════════════════════════════════════════════════════════════════════════

  core-storage:
    name: "infra: core-storage"
    needs: [core-common]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-storage:detekt :infra:core-storage:lintDebug :infra:core-storage:testDebugUnitTest :infra:core-storage:assembleRelease :infra:core-storage:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-storage
          path: |
            infra/core-storage/build/reports/tests/
            infra/core-storage/build/reports/detekt/
            infra/core-storage/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-storage
          path: infra/core-storage/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-storage
          path: infra/core-storage/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  core-device:
    name: "infra: core-device"
    needs: [core-common]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-device:detekt :infra:core-device:lintDebug :infra:core-device:testDebugUnitTest :infra:core-device:assembleRelease :infra:core-device:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-device
          path: |
            infra/core-device/build/reports/tests/
            infra/core-device/build/reports/detekt/
            infra/core-device/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-device
          path: infra/core-device/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-device
          path: infra/core-device/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  core-network:
    name: "infra: core-network"
    needs: [core-common]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-network:detekt :infra:core-network:lintDebug :infra:core-network:testDebugUnitTest :infra:core-network:assembleRelease :infra:core-network:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-network
          path: |
            infra/core-network/build/reports/tests/
            infra/core-network/build/reports/detekt/
            infra/core-network/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-network
          path: infra/core-network/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-network
          path: infra/core-network/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  core-location:
    name: "infra: core-location"
    needs: [core-common]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-location:detekt :infra:core-location:lintDebug :infra:core-location:testDebugUnitTest :infra:core-location:assembleRelease :infra:core-location:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-location
          path: |
            infra/core-location/build/reports/tests/
            infra/core-location/build/reports/detekt/
            infra/core-location/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-location
          path: infra/core-location/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-location
          path: infra/core-location/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  core-camera:
    name: "infra: core-camera"
    needs: [core-common]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-camera:detekt :infra:core-camera:lintDebug :infra:core-camera:testDebugUnitTest :infra:core-camera:assembleRelease :infra:core-camera:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-camera
          path: |
            infra/core-camera/build/reports/tests/
            infra/core-camera/build/reports/detekt/
            infra/core-camera/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-camera
          path: infra/core-camera/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-camera
          path: infra/core-camera/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 3: INFRA COMPOSTA (Depende de core-storage)
  # ═════════════════════════════════════════════════════════════════════════════

  core-database:
    name: "infra: core-database"
    needs: [core-common, core-storage]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-database:detekt :infra:core-database:lintDebug :infra:core-database:testDebugUnitTest :infra:core-database:assembleRelease :infra:core-database:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-database
          path: |
            infra/core-database/build/reports/tests/
            infra/core-database/build/reports/detekt/
            infra/core-database/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-database
          path: infra/core-database/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-database
          path: infra/core-database/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  core-analytics:
    name: "infra: core-analytics"
    needs: [core-common, core-storage]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-analytics:detekt :infra:core-analytics:lintDebug :infra:core-analytics:testDebugUnitTest :infra:core-analytics:assembleRelease :infra:core-analytics:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-analytics
          path: |
            infra/core-analytics/build/reports/tests/
            infra/core-analytics/build/reports/detekt/
            infra/core-analytics/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-analytics
          path: infra/core-analytics/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-analytics
          path: infra/core-analytics/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  core-testing:
    name: "infra: core-testing"
    needs: [core-common, core-network]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :infra:core-testing:detekt :infra:core-testing:lintDebug :infra:core-testing:testDebugUnitTest :infra:core-testing:assembleRelease :infra:core-testing:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-testing
          path: |
            infra/core-testing/build/reports/tests/
            infra/core-testing/build/reports/detekt/
            infra/core-testing/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-testing
          path: infra/core-testing/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-testing
          path: infra/core-testing/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 4: BUNDLES E GUARDA-CHUVA
  # ═════════════════════════════════════════════════════════════════════════════

  core-umbrella:
    name: "infra: core (umbrella)"
    needs:
      - core-common
      - core-storage
      - core-device
      - core-network
      - core-location
      - core-camera
      - core-database
      - core-analytics
      - core-testing
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes, Build e Dokka
        run: ./gradlew :infra:core:detekt :infra:core:lintDebug :infra:core:testDebugUnitTest :infra:core:assembleRelease :infra:core:assembleDebug dokkaHtml
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-core-umbrella
          path: |
            infra/core/build/reports/tests/
            infra/core/build/reports/detekt/
            infra/core/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: 📚 Upload Dokka Documentation
        uses: actions/upload-artifact@v4
        with:
          name: dokka-api-documentation
          path: |
            **/build/dokka/html/
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-core-umbrella
          path: infra/core/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-core-umbrella
          path: infra/core/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  bundle-persistence:
    name: "bundle: persistence"
    needs: [core-common, core-storage, core-database]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :bundle:persistence:detekt :bundle:persistence:lintDebug :bundle:persistence:testDebugUnitTest :bundle:persistence:assembleRelease :bundle:persistence:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-bundle-persistence
          path: |
            bundle/persistence/build/reports/tests/
            bundle/persistence/build/reports/detekt/
            bundle/persistence/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-bundle-persistence
          path: bundle/persistence/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-bundle-persistence
          path: bundle/persistence/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  bundle-networking:
    name: "bundle: networking"
    needs: [core-common, core-device, core-network]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :bundle:networking:detekt :bundle:networking:lintDebug :bundle:networking:testDebugUnitTest :bundle:networking:assembleRelease :bundle:networking:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-bundle-networking
          path: |
            bundle/networking/build/reports/tests/
            bundle/networking/build/reports/detekt/
            bundle/networking/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-bundle-networking
          path: bundle/networking/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-bundle-networking
          path: bundle/networking/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  bundle-presentation:
    name: "bundle: presentation"
    needs: [core-common, core-device, core-camera]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :bundle:presentation:detekt :bundle:presentation:lintDebug :bundle:presentation:testDebugUnitTest :bundle:presentation:assembleRelease :bundle:presentation:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-bundle-presentation
          path: |
            bundle/presentation/build/reports/tests/
            bundle/presentation/build/reports/detekt/
            bundle/presentation/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-bundle-presentation
          path: bundle/presentation/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-bundle-presentation
          path: bundle/presentation/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  bundle-hardware:
    name: "bundle: hardware"
    needs: [core-common, core-device, core-location]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Detekt, Lint, Testes e Build
        run: ./gradlew :bundle:hardware:detekt :bundle:hardware:lintDebug :bundle:hardware:testDebugUnitTest :bundle:hardware:assembleRelease :bundle:hardware:assembleDebug
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-bundle-hardware
          path: |
            bundle/hardware/build/reports/tests/
            bundle/hardware/build/reports/detekt/
            bundle/hardware/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Release AAR
        uses: actions/upload-artifact@v4
        with:
          name: release-aar-bundle-hardware
          path: bundle/hardware/build/outputs/aar/*-release.aar
          if-no-files-found: error
      - name: ⬆️ Upload Debug AAR
        uses: actions/upload-artifact@v4
        with:
          name: debug-aar-bundle-hardware
          path: bundle/hardware/build/outputs/aar/*-debug.aar
          if-no-files-found: ignore

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 5: SAMPLE APP (Validação de Integração Completa)
  # ═════════════════════════════════════════════════════════════════════════════

  sample-app:
    name: "app: sample-app"
    needs: [core-umbrella]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 🔍 Lint, Testes e Build do App
        run: ./gradlew :app:lintDebug :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease
      - name: 📊 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-sample-app
          path: |
            app/build/reports/tests/
            app/build/reports/lint-results*.html
          if-no-files-found: ignore
      - name: ⬆️ Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: ignore

  coverage-report:
    name: "📊 Jacoco Root Coverage Report"
    needs: [core-umbrella]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with: { fetch-depth: 0, lfs: true }
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 📈 Generate Jacoco Root Coverage Report
        run: ./gradlew jacocoRootReport
      - name: 📊 Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-root-coverage-report
          path: build/reports/jacoco/jacocoRootReport/
          if-no-files-found: ignore

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 6: PUBLICAÇÃO E DEPLOY POR CAMADA (Apenas em Push para Master)
  # ═════════════════════════════════════════════════════════════════════════════

  publish-infra:
    name: "🚀 Deploy Infra (Atomic Modules)"
    needs:
      - core-umbrella
      - sample-app
      - coverage-report
      - codeql-analysis
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment:
      name: production-infra
      url: https://github.com/${{ github.repository }}/releases
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Detect changed library modules
        id: changed_modules
        uses: tj-actions/changed-files@v47
        with:
          files: |
            infra/**
            bundle/**

      - name: Setup Android & Java Environment
        if: steps.changed_modules.outputs.any_changed == 'true'
        uses: ./.github/actions/setup-android-env

      - name: 🏷️ Calculate Semantic Version (SemVer)
        if: steps.changed_modules.outputs.any_changed == 'true'
        id: semver
        run: |
          LATEST_TAG=$(git describe --tags --match "v[1-9]*.[0-9]*.[0-9]*" --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LATEST_TAG" ]; then
            NEW_TAG="v1.2.0"
          else
            VERSION=${LATEST_TAG#v}
            MAJOR=$(echo "$VERSION" | cut -d. -f1)
            MINOR=$(echo "$VERSION" | cut -d. -f2)
            PATCH=$(echo "$VERSION" | cut -d. -f3)

            COMMITS=$(git log "${LATEST_TAG}..HEAD" --pretty=format:"%s")
            if echo "$COMMITS" | grep -qE "(BREAKING CHANGE|feat!:)"; then
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
            elif echo "$COMMITS" | grep -qE "^feat(\(.*\))?:"; then
              MINOR=$((MINOR + 1))
              PATCH=0
            else
              PATCH=$((PATCH + 1))
            fi
            NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
          fi
          NEW_VERSION=${NEW_TAG#v}
          echo "Calculated SemVer Tag: $NEW_TAG (version: $NEW_VERSION)"
          echo "tag_name=$NEW_TAG" >> $GITHUB_OUTPUT
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: 📦 Publish Infra modules to GitHub Packages
        if: steps.changed_modules.outputs.any_changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew :infra:core-common:publish :infra:core-storage:publish :infra:core-device:publish :infra:core-network:publish :infra:core-database:publish :infra:core-location:publish :infra:core-camera:publish :infra:core-analytics:publish :infra:core-testing:publish :infra:core:publish -Pversion="${{ steps.semver.outputs.version }}"

      - name: ⬇️ Download Infra Release AARs
        if: steps.changed_modules.outputs.any_changed == 'true'
        uses: actions/download-artifact@v4
        with:
          pattern: release-aar-core-*
          merge-multiple: true
          path: ./infra-release-aars

      - name: Configure GitHub Access
        if: steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

      - name: Create Git Tag
        if: steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ steps.semver.outputs.tag_name }}"
          git tag -a "$TAG_NAME" -m "Release $TAG_NAME"
          git push origin "$TAG_NAME"

      - name: Create Release with Infra Artifacts
        if: steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ steps.semver.outputs.tag_name }}"
          mapfile -t AAR_FILES < <(find ./infra-release-aars -type f -name "*-release.aar")
          if [ ${#AAR_FILES[@]} -gt 0 ]; then
            gh release create "$TAG_NAME" \
              --title "Release $TAG_NAME" \
              --notes "Release automático gerado pela esteira CI/CD (SemVer).
              Artefatos de infraestrutura publicados no GitHub Packages:
              - br.com.wgc:core-common
              - br.com.wgc:core-storage
              - br.com.wgc:core-device
              - br.com.wgc:core-network
              - br.com.wgc:core-database
              - br.com.wgc:core-location
              - br.com.wgc:core-camera
              - br.com.wgc:core-analytics
              - br.com.wgc:core-testing
              - br.com.wgc:core-android-native (guarda-chuva)
              Mudanças detectadas em: ${{ steps.changed_modules.outputs.all_changed_files }}." \
              "${AAR_FILES[@]}"
          else
            gh release create "$TAG_NAME" \
              --title "Release $TAG_NAME" \
              --notes "Release automático. Módulos de infraestrutura publicados no GitHub Packages.
              Mudanças detectadas em: ${{ steps.changed_modules.outputs.all_changed_files }}."
          fi

  publish-bundles:
    name: "🚀 Deploy Bundles"
    needs:
      - publish-infra
      - bundle-persistence
      - bundle-networking
      - bundle-presentation
      - bundle-hardware
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment:
      name: production-bundles
      url: https://github.com/${{ github.repository }}/releases
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Detect changed library modules
        id: changed_modules
        uses: tj-actions/changed-files@v47
        with:
          files: |
            infra/**
            bundle/**

      - name: Setup Android & Java Environment
        if: steps.changed_modules.outputs.any_changed == 'true'
        uses: ./.github/actions/setup-android-env

      - name: 🏷️ Get Current Release Version
        if: steps.changed_modules.outputs.any_changed == 'true'
        id: semver
        run: |
          LATEST_TAG=$(git describe --tags --match "v[1-9]*.[0-9]*.[0-9]*" --abbrev=0 2>/dev/null || echo "v1.2.0")
          VERSION=${LATEST_TAG#v}
          echo "Current Release Version: $VERSION ($LATEST_TAG)"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag_name=$LATEST_TAG" >> $GITHUB_OUTPUT

      - name: 📦 Publish Bundles to GitHub Packages
        if: steps.changed_modules.outputs.any_changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew :bundle:persistence:publish :bundle:networking:publish :bundle:presentation:publish :bundle:hardware:publish -Pversion="${{ steps.semver.outputs.version }}"

      - name: ⬇️ Download Bundle Release AARs
        if: steps.changed_modules.outputs.any_changed == 'true'
        uses: actions/download-artifact@v4
        with:
          pattern: release-aar-bundle-*
          merge-multiple: true
          path: ./bundle-release-aars

      - name: Attach Bundle AARs to Release
        if: steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LATEST_TAG=$(git describe --tags --match "v[1-9]*.[0-9]*.[0-9]*" --abbrev=0 2>/dev/null || echo "v1.2.0")
          mapfile -t AAR_FILES < <(find ./bundle-release-aars -type f -name "*-release.aar")
          if [ ${#AAR_FILES[@]} -gt 0 ]; then
            gh release upload "$LATEST_TAG" "${AAR_FILES[@]}" --clobber
          fi

  # ═════════════════════════════════════════════════════════════════════════════
  # NÍVEL 7: DEPLOY DE DOCUMENTAÇÃO DOKKA NO GITHUB PAGES
  # ═════════════════════════════════════════════════════════════════════════════

  deploy-docs:
    name: "🌐 Deploy Dokka Docs to GitHub Pages"
    needs: [publish-bundles]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Android & Java Environment
        uses: ./.github/actions/setup-android-env
      - name: 📚 Generate Dokka HTML
        run: ./gradlew dokkaHtml
      - name: 📁 Consolidate Documentation Pages
        run: |
          mkdir -p ./site-docs
          cp -r infra/core/build/dokka/html/* ./site-docs/ || true
          if [ ! -f ./site-docs/index.html ]; then
            find . -type d -path "*/build/dokka/html" -exec cp -r {}/* ./site-docs/ \;
          fi
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './site-docs'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
