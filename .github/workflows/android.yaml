name: Android Publish Lib CoreAndroidNative

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build_Test_and_Publish:

    name: Build, Test and Publish
    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          lfs: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Detect changed modules
        id: changed_modules
        uses: tj-actions/changed-files@v47
        with:
          files: |
            core*/**

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🤖 Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: 🔍 Run Detekt Static Analysis
        run: ./gradlew detekt

      - name: 🔎 Run Android Lint
        run: ./gradlew lintDebug

      - name: 🧪 Run Unit Tests
        run: ./gradlew testDebugUnitTest

      - name: Build with Gradle
        run: ./gradlew build

      - name: ⬆️ Upload Debug AARs as Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: debugs-aars
          path: |
            ./*/build/outputs/aar/*-debug.aar

      - name: ⬆️ Upload Release AARs as Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: release-aars
          path: |
            ./*/build/outputs/aar/*-release.aar

      # ─── Publicação dos Módulos ─────────────────────────────────────────────

      - name: 📦 Publish all modules to GitHub Packages
        if: github.event_name == 'push' && github.ref == 'refs/heads/master' && steps.changed_modules.outputs.any_changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew publish

      # ─── Tag e Release ─────────────────────────────────────────────────────

      - name: Configure GitHub Access
        if: github.event_name == 'push' && github.ref == 'refs/heads/master' && steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

      - name: Create Git Tag
        if: github.event_name == 'push' && github.ref == 'refs/heads/master' && steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="v0.0.${{ github.run_number }}"
          git tag -a "$TAG_NAME" -m "Release $TAG_NAME"
          git push origin "$TAG_NAME"

      - name: Create Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/master' && steps.changed_modules.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="v0.0.${{ github.run_number }}"
          gh release create "$TAG_NAME" \
            --title "Release $TAG_NAME" \
            --notes "Release automático. Artefatos publicados individualmente:
            - br.com.wgc:core-common
            - br.com.wgc:core-storage
            - br.com.wgc:core-device
            - br.com.wgc:core-network
            - br.com.wgc:core-ui
            - br.com.wgc:core-database
            - br.com.wgc:core-location
            - br.com.wgc:core-camera
            - br.com.wgc:core-analytics
            - br.com.wgc:core-android-native (guarda-chuva)
            Mudanças detectadas em: ${{ steps.changed_modules.outputs.all_changed_files }}." \
            ./core/build/outputs/aar/core-release.aar \
            ./core-common/build/outputs/aar/core-common-release.aar \
            ./core-storage/build/outputs/aar/core-storage-release.aar \
            ./core-device/build/outputs/aar/core-device-release.aar \
            ./core-network/build/outputs/aar/core-network-release.aar \
            ./core-ui/build/outputs/aar/core-ui-release.aar \
            ./core-database/build/outputs/aar/core-database-release.aar \
            ./core-location/build/outputs/aar/core-location-release.aar \
            ./core-camera/build/outputs/aar/core-camera-release.aar \
            ./core-analytics/build/outputs/aar/core-analytics-release.aar
